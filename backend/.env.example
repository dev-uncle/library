CONNECTION_PORT = 5000

# Use this Database Connection_URL to run on LocalHost machine (If you dont use docker , use this)
CONNECTION_URL = mongodb+srv://dbFermin:<db_password>@cluster0.q4emfmt.mongodb.net/?appName=Cluster0
# CONNECTION_URL =  mongodb://localhost:27017/librarymgmtsystem

# Use this Database Connection URL if you run the application on Docker (This URL is used to connected Docker Container to Localhost MongoDB) 
# CONNECTION_URL=mongodb://host.docker.internal:27017/librarymgmtsystem


JWT_SECRET = n?s!6m=F$Kg%AgTJ-gN5)Hn~YtllC.AT

JWT_REFRESH_SECRET = &h+D?fhvq$cC6WoR

JWT_LIFE = 30m

JWT_REFRESH_LIFE = 365d


# FOR EMAIL Verification
# For Local Development: You can use your Google account's App Password and Email.
EMAIL_USERNAME = "rocendoastillero@gmail.com"
EMAIL_PASSWORD = "dfexlgjysmonxinn"  

# For Production (e.g., Railway): Since direct SMTP ports are blocked, use Gmail API via OAuth2 (HTTPS).
# 1. Create a Google Cloud Project, enable Gmail API.
# 2. Configure OAuth consent screen and create OAuth Client credentials.
# 3. Generate a refresh token.
GMAIL_CLIENT_ID = "your-google-oauth-client-id"
GMAIL_CLIENT_SECRET = "your-google-oauth-client-secret"
GMAIL_REFRESH_TOKEN = "your-google-oauth-refresh-token"